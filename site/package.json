{
  "name": "kmsg-readme-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node build.mjs",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "dependencies": {
    "@phosphor-icons/core": "^2.1.1",
    "marked": "18.0.7",
    "sanitize-html": "2.17.6"
  }
}
